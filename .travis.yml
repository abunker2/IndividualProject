language: ruby
cache: bundler
bundler_args: --without production

rvm:
 - 2.7.0
 
before_install:
 - gem install bundler:2.2.11

script:
 - bundle exec rspec
 
 deploy:
  provider: heroku
  api_key: d7a4a675-b01c-4db5-b1e2-851351415910
  app: IndividualProject
  run: "rake db:migrate"